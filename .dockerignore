.git
.github
.venv
venv
__pycache__
*.py[cod]
.mypy_cache
.ruff_cache
.pytest_cache
.coverage
htmlcov
# Root-anchored: bare `models` would also strip backend/src/parallax/db/models/.
/data
/volumes
/models
.env
.env.*
!.env.example
# Note: backend/README.md must stay in the context - backend/pyproject.toml
# declares `readme`, and the hatchling build fails without it.
docs

# Frontend build inputs are copied explicitly by docker/frontend/Dockerfile.
node_modules
frontend/node_modules
frontend/dist
frontend/.vite

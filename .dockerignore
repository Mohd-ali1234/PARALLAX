.git
.github
.venv
venv
__pycache__
*.py[cod]
.mypy_cache
.ruff_cache
.pytest_cache
.coverage
htmlcov
# Root-anchored: bare `models` would also strip src/parallax/db/models/.
/data
/volumes
/models
.env
.env.*
!.env.example
# NOT README.md: pyproject declares `readme = "README.md"`, so the hatchling
# build in docker/api/Dockerfile fails without it in the context.
docs

# Frontend build inputs are copied explicitly by docker/frontend/Dockerfile.
node_modules
frontend/node_modules
frontend/dist
frontend/.vite
